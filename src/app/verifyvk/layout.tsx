import type { Metadata } from "next";
export const metadata: Metadata = { title: "Verify VK — Storage Proof" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
