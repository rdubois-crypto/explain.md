import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "ZK Clear Signing — Hardware",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
